#!/usr/bin/env bash
set -euo pipefail

echo "This helper is a placeholder for updating the vendored pose-contact-spec schema."
echo "Steps:"
echo "1. Replace schemas/pose-contact-spec/schema.yaml with the updated snapshot."
echo "2. Update the schema_version in index.js and schema.yaml."
echo "3. Document changes in README.md if needed."
