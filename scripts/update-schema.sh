#!/usr/bin/env bash
set -euo pipefail

echo "This helper is a placeholder for updating the vendored pose-contact-spec schema."
echo "Steps:"
echo "1. Replace schemas/pose-contact-spec/pose-contact-spec.schema.json with the updated snapshot."
echo "2. Update SCHEMA_MANIFEST.json with the new version and source info."
echo "3. Update the schema_version in index.js and README.md if needed."
